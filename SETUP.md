# Setup

Use this repository as the profile repository named `xavierkingsleen-11`.

Upload the full structure, including `.github/workflows/contribution-snake.yml`.

`preview.html` is a local browser preview of the generated visual. GitHub renders `README.md`.

The main visual is generated in `assets/profile-dashboard.svg`, while the hacker image is separately processed into `assets/hacker-blended.png` and `assets/hacker-glitch.gif`.

Run the contribution-snake workflow manually once after the first push.
