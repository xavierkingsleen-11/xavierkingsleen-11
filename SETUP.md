# GitHub Profile README Package

1. Use this repository as the profile repository named `xavierkingsleen-11`.
2. Keep the included `README.md`, `assets/` folder, and `.github/workflows/snake.yml`.
3. Push the files to GitHub.
4. GitHub Actions will generate the contribution snake into the `output` branch on its scheduled run (or run it manually from Actions).
5. The README's GitHub activity, contribution graph, badges, and snake are designed to update from GitHub/external live services.

The uploaded hacker image has been converted into `assets/xavier-hacker-glitch.gif` with a dark blended background and a short glitch/CCTV-style burst on a repeating loop.
